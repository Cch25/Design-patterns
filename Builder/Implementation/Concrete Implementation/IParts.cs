namespace Builder.Implementation.ConcreteImplementation
{
    public interface IParts
    {
        void AddMirrors(bool hasFrame);
        void AddWheels(int wheelsNo);
        void AddDoors(int doorsNo);
       
    }
}
